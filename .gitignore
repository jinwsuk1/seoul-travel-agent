# 가상환경 (팀원마다 경로가 다를 수 있음)
venv/
env/
.venv
multimodal_images
# 보안 (Gemini API 키 등)
.env

# 로컬 벡터 DB 폴더 (용량이 크고 데이터가 꼬일 수 있으니 제외)
# 대신 이 폴더는 압축해서 따로 팀원들에게 전달하세요.
qdrant_local_db/

# 원본 데이터 (필요 시 제외, 60MB 이상이면 깃허브 업로드가 제한될 수 있음)
*.csv

# 파이썬 캐시 및 시스템 파일
__pycache__/
*.pyc
.DS_Store